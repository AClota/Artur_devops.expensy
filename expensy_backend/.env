PORT=8706

# --- Mongo en local (NO docker-compose): usa localhost y authSource=admin ---
MONGO_URI=mongodb://root:example@localhost:27017/expensy?authSource=admin
# Si tu código usa DATABASE_URI en vez de MONGO_URI, duplica:
DATABASE_URI=mongodb://root:example@localhost:27017/expensy?authSource=admin

# --- Redis en local con contraseña ---
# URL completa (recomendado):
REDIS_URL=redis://:someredispassword@localhost:6379
# Por si tu código usa host/port/password en lugar de URL:
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=someredispassword
